#include <stdio.h>  
int main() { 
    int num1 = 5; 
    int num2 = 10; 
    int sum = num1 + num2; 
    printf("The sum is: %d\n", sum);  
    return 0; 
}
    Show output file with extension " objectfile.o" generated by C compiler along with Makefile 
    (point penalty for adding extra commands other than necessary)